{
  "name": "acne-backend",
  "version": "1.0.0",
  "description": "Backend for Acne Severity Detector",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "preinstall": "rm -rf node_modules package-lock.json", 
    "prestart": "echo '⚡ Loading Model...'",
    "start": "node server.js",
    "dev": "nodemon --experimental-json-modules server.js",
    "postinstall": "mkdir -p uploads && echo '✅ Post-install tasks completed.'",
    "build": "echo '🚀 Building backend...'",
    "prepare": "echo '📦 Backend is ready for deployment!'"
  },
  "dependencies": {
    "@tensorflow/tfjs-node": "^4.22.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
